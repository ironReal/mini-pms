/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.eomcs.pms.app;

import com.eomcs.pms.list.LinkedList;

import static com.eomcs.pms.utilities.StringUtils.join;
import static com.eomcs.pms.utilities.StringUtils.split;
import static com.eomcs.pms.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
